import { User } from "../user";

export interface SellerProfileParams {
    user: string;
}

export interface ProfileUserProps {
    // Current user profile usually doesn't have props as it uses authStore
}
